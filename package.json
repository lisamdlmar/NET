{
  "name": "NET",
  "version": "1.0.0",
  "main": "server/app.js",
  "scripts": {
    "start": "yarn && tsc && nodemon server/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.1",
    "morgan": "^1.9.1",
    "tslint": "^5.20.0",
    "typescript": "^3.6.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^1.19.2"
  }
}
